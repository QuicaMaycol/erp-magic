// Archivo para compilar en movil/escritorio sin romper
void downloadWeb(List<int> bytes, String fileName) {
  // Se implementará con path_provider en el futuro si es necesario para móvil
  print("Descarga no soportada nativamente sin path_provider en este helper.");
}
